/*
import { introPage } from './app.po';

describe('props App', () => {
  let page: UNPropsPage;

  beforeEach(() => {
    page = new UNPropsPage();
  });

  it('should display message saying app works', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('app works!');
  });
});
*/
